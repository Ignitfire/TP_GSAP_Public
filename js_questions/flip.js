// recuperer le plugin Flip de GSAP

// puis recuperer les elements du DOM qui ont la classe box dans un tableau squares
const squares = gsap.utils.toArray(".box");

function doFlip() {
  // recuperer l'etat initial
  
  // faire les changements sur le DOM ou sur le style (ici on inverse les elements du tableau squares)
  
  // Animer de l'etat initial à l'etat final

}

// Etant donné un tableau de 2 elements, inverser leur ordre dans le DOM
function swap([a, b]) {
}

// ajouter un EventListener sur le document qui declenche la fonction doFlip au click